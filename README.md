### Pour installer sur une machine Python3.11 avec pdm, se placer sous la racine du projet puis faire:  <br>
```bash
pdm install  
pdm run pip install tensorflow 
pdm run jupyter lab dim-reduc.ipynb  
```
